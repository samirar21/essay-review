// Placeholder — wired up to Resend in the email notifications step.
export async function sendFeedbackReadyEmail(essayId: string) {
  console.log(`[email] feedback ready for essay ${essayId} (Resend not wired yet)`);
}
